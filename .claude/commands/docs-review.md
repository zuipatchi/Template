---
name: docs-review
description: docs/ 配下のドキュメント・CLAUDE.md・README.md の「内部品質」をレビューして修正まで完結する。矛盾・重複・壊れたリンク・古い記述・誤字・構成の崩れを検出し、自己判断で修正してコミットする。「ドキュメントレビュー」「docs に変なところがないか見て」「文書の重複をまとめて」「ドキュメントを整理して」といった依頼でトリガーする。コードとの乖離チェックは sync-docs の責務なので、そちらが目的なら sync-docs を使う。
---

# docs-review

ドキュメントの**内部品質**を検出・修正するスキル。コードは見ず、ドキュメント同士・ドキュメント内の整合性に責務を絞る。

> **sync-docs との違い**: sync-docs は「コードとドキュメントの乖離」を扱う。docs-review は「ドキュメント内部の品質」（矛盾・重複・リンク切れ・誤字・構成）を扱う。コードと突き合わせたいなら sync-docs を使う。

## フロー

1. **調査** — Explore エージェントにドキュメントを横断的にレビューさせる
2. **判断** — 検出した問題を自分で仕分けする（ユーザー確認は挟まない）
3. **修正** — 修正すると決めた箇所を修正する
4. **コミット** — 変更をコミットする
5. **報告** — 直した内容と見送った内容をまとめて報告する

**このスキルは途中でユーザーに確認を求めず、修正とコミットまで自分で完結する。**

---

## Phase 1: 調査

Explore エージェントを spawn して以下を調査させる。**対象は `docs/` 配下の全 .md・`CLAUDE.md`・`README.md`。コードは読まない**。

`CLAUDE.md`・`README.md`・`docs/` は役割が近く内容が重複・矛盾しやすいので、**3者をまたいだ横断チェックを必ず行う**（例: 同じアーキテクチャ説明が CLAUDE.md と docs/architecture.md の両方にある、README とドキュメントでセットアップ手順が食い違う、CLAUDE.md の「主要ファイルの場所」と docs/ のリンク先がズレている等）。

エージェントへの指示（検出カテゴリ）：

| カテゴリ | 検出内容 |
|---|---|
| 矛盾 | 同じ事項について別々のドキュメント（または同一ドキュメント内）で食い違う記述がないか（数値・命名・手順・方針など） |
| 重複 | 同じ説明・同じ表・同じコード例が複数箇所に重複していないか。一方に集約して他方からリンクすべき箇所はどこか |
| リンク切れ | `[テキスト](パス)` のリンク先ファイル・アンカーが実在するか（相対パス・`#見出し` アンカー含む） |
| 古い記述 | 「TODO」「未実装」「予定」など、もう実態と合っていなさそうな記述。日付・バージョン番号の古さ |
| 構成の崩れ | 見出し階層の飛び（h2 の次にいきなり h4 等）、目次と実際の見出しのズレ、表のカラム不揃い |
| 誤字・表記ゆれ | 明らかな誤字、同一用語の表記ゆれ（例: "Addressable" と "Addressables"、全角/半角の不統一） |

各問題について **「ファイルパスと行番号」「カテゴリ」「問題の要約」「修正案」「確実さ（明確／要判断）」** を報告させる。

> リンク切れの確認は Glob ツールで実ファイルの存在を、Grep ツールで見出しアンカーの存在を確かめる（推測で「切れている」と書かない）。

---

## Phase 2: 判断

エージェントの報告を受けて、**各項目を修正するかどうか自分で決める**。ユーザーに聞かない。

- **明確**（誤字・リンク切れ・表記ゆれ・単純な構成崩れ）→ そのまま修正する
- **要判断**（矛盾のどちらを「正」とするか・重複の集約方針・構成の組み替え）→ まず自分で裏取りして決める：
  - `git log` / `git log -p <file>` で新しく書かれた側・意図的に変えられた側を確認する（新しい記述の方が「正」の可能性が高い）
  - 実装が絡む矛盾なら該当コードを読んで実態に合わせる（このスキルは通常コードを読まないが、矛盾の決着に必要な範囲だけは読んでよい）
  - 重複は「どちらが主題のドキュメントか」で集約先を決める（例: アーキテクチャの詳細は docs/architecture.md、CLAUDE.md 側は要点＋リンク）
- 裏取りしても決められない項目 → **手を付けずに「見送り」として記録する**。中途半端に片方へ寄せない

見送りにする基準（迷ったらこれに該当するかで判断）：

- どちらが正しいかを決めるのに、ドキュメント・コード・git 履歴のいずれからも根拠が得られない
- 修正が「品質の修正」ではなく「仕様・方針の決定」になる（プロダクトの方針をこちらで決めることになる）
- 大規模な章の削除・ファイル分割など、ドキュメント全体の構成を変える判断が必要

---

## Phase 3: 修正

修正すると決めた箇所を Edit ツールで修正する。

修正の方針：
- **重複の集約** → 最も適切な 1 箇所に内容を残し、他方は要約＋該当箇所へのリンクに置き換える（情報を二重管理しない）
- **リンク切れ** → 正しいパス・アンカーに直す。リンク先が消えていれば記述ごと見直す
- **矛盾** → Phase 2 で「正」と決めた側に統一する
- **誤字・表記ゆれ** → 既存ドキュメントで多数派の表記に合わせる

注意：
- 文体・スタイルは既存ドキュメントに合わせる
- 修正範囲は検出した問題のみ。無関係な文言改善・リライトはしない
- 内容を消すときは「削除済み」コメントを残さず記述ごと消す

---

## Phase 4: コミット

修正が完了したらコミットする。複数ファイルを修正した場合も 1 コミットにまとめてよい。

コミットメッセージ形式：
```
docs: <変更の要点>
```

例：
```
docs: ドキュメント間の重複を集約しリンク切れ・表記ゆれを修正
```

修正すべき箇所が 1 つも無かった場合はコミットせず、その旨を報告して終了する。

---

## Phase 5: 報告

最後にまとめて報告する。**確認ではなく事後報告**なので、簡潔に：

- 直した項目（カテゴリ別・ファイル単位でまとめる）
- 見送った項目とその理由（ユーザーの判断が必要なものはここに集める）
- コミットハッシュ

---

## 注意事項

- このスキルは**ドキュメントの内部品質だけ**を見る。コードとの整合性は確認しない（それは sync-docs の責務。ただし矛盾の決着に必要な範囲でコードを読むのは可）
- リンク・アンカーの実在は必ずツールで確認する。推測で判断しない
- 判断に迷ったら「ユーザーに聞く」のではなく「見送って報告する」。手を付けた箇所は必ず根拠を持って直しきる
